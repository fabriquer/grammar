#
# RUN: %parse %s > %t
# RUN: %check %t %s
#

# CHECK: (file

# CHECK: (value foo ={{.*}} (literal 'a string literal'{{[)]+}} ;)
foo = 'a string literal';

# CHECK: (value bar : (type {{.*}} int){{[)]+}} = (expression {{.*}}(literal 42{{[)]+}} ;)
bar:int = 42;

# CHECK: (value baz =
# CHECK:   list [
# CHECK:     (literal 1)
# CHECK:     (literal 2)
# CHECK:     (literal 3)
# CHECK:   ]
# CHECK: ;)
baz = [ 1 2 3 ];


# CHECK: (value wibble =
# CHECK:   list [
# CHECK:     (literal true)
# CHECK:     (literal false)
# CHECK:   ]
# CHECK: ;)
wibble = [ true false ];
